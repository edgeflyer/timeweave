# QuestOS 开发规划

> 本文档基于 `规划.docx`（QuestOS v1.0 总纲）整理，把产品哲学拆成可落地的工程实施计划。  
> 任何功能、API、数据库表、Agent 工具的设计都必须围绕本文档展开，**不轻易偏离**。

---

## 1. 项目定位

**QuestOS** = Go 后端 + Agent 驱动的 **自适应个人与多人时间管理系统**。

- **不是** 传统 Todo List，也**不是** 单纯 AI 聊天应用。
- 核心目标：根据长期目标 + 临时需求 + 可用时间 + 历史执行数据，**自动生成每日计划**，再以 **Timer 真实耗时** 持续调整计划。

### 1.1 闭环（产品公式）
```
Plan        = 计划
Observe      = 观察（Timer 记录）
Learn        = 学习（修正 estimated_duration）
Replan       = 重新规划
```

具体数据流：
用户目标/临时需求
  → 自然语言理解
  → 任务结构化
  → 时间约束分析
  → 每日计划生成
  → 任务执行
  → Timer 记录真实耗时
  → 完成 / 超时 / 暂停 / 跳过
  → 执行数据分析
  → 重新估计任务耗时
  → 动态调整剩余计划
  → 影响未来每日计划

### 1.2 七大核心模块（贯穿所有阶段）
1. Go Backend
3. Scheduler Engine
5. Timer Engine
7. Execution History
9. Adaptive Duration Estimation
11. Agent Tool System
13. Dynamic Replanning
15. Team Collaboration

---

## 2. 关键设计原则

| 维度 | 原则 |
|------|------|
| 职责分离 | **Agent 负责理解用户，Scheduler 负责决定时间**，二者严格解耦 |
| 状态权威 | 所有真实状态（Timer、Task Status、冲突、依赖）必须在 Go 服务端，客户端只做展示 |
| DB 边界 | Agent **不能**直接读写 DB，只能通过 Go Tool 调用 |
| 不可信 LLM | Scheduler 不能完全依赖 LLM，必须基于确定性规则 + 约束求解 |
| 不加冗余 | 任何新功能必须服务于：输入 / 规划 / 执行 / 计时 / 分析 / 重规划 / 协作 之一，否则不做 |
| Timer 服务端化 | Timer 真实状态在服务端，App 杀掉 / 断网 / 换设备均可恢复与同步 |

---

## 3. 核心模型

### 3.1 Task 类型
- `LONG_TERM` 长期
- `TEMPORARY` 临时
- `FIXED_TIME` 固定时段
- `RECURRING` 循环
- `TEAM` 团队
- `HABIT` 习惯

### 3.2 Task 字段
```
id, user_id, goal_id, milestone_id, title, description,
type, priority, estimated_duration, deadline, status,
created_at, updated_at
```

### 3.3 Task 状态机
```
PENDING → SCHEDULED → RUNNING → COMPLETED
                  ↘ PAUSED ↗
                  ↘ SKIPPED
                  ↘ CANCELLED
```

### 3.4 Goal 三层结构
```
Goal
 └─ Milestone
     └─ Task
```
- Agent 可辅助拆解，但**必须落库**，不能只活在聊天记录里。

### 3.5 DailyPlan ≠ Task List，而是：
- 当天可用时间 + 当天 Task + Calendar + Scheduler 输出

### 3.6 TaskExecution
每执行一次就保存一条，用于学习用户行为：
```
task_id, start_time, end_time,
planned_duration, actual_duration, pause_duration, overtime, result
```

### 3.7 UserTimeProfile
按 `task_type`（Coding / Reading / Writing / Exercise / Meeting / Study）维护：
- 初始预计时长
- 最近 N 次实际平均
- 滚动修正后的新预计
- 后续扩展：时间段（morning/evening）、能量等级等维度

### 3.8 ScheduleBlock（Scheduler 输出）
```
14:00 - 15:10  JWT Middleware
15:10 - 15:25  Break
15:25 - 16:15  User Service
16:15 - 16:45  English
```

---

## 4. Scheduler Engine 设计

### 4.1 输入（必须考虑）
- available_time
- deadline
- priority
- estimated_duration
- task_dependencies
- fixed_schedule
- preferred_time
- maximum_focus_duration
- break_duration
- historical_duration
- task_type
- long_term_goal_priority
- team_constraint

### 4.2 输出
有序 `ScheduleBlock[]`，含冲突检测与自动补 Break。

### 4.3 触发重排的条件（Replanning）
- 任务超时
- 任务提前完成
- 任务跳过 / 新增 / 删除
- 可用时间变化
- Calendar 变化
- Team Task 变化

### 4.4 实现策略
**Phase 3 先做确定性规则版本**，不接 LLM；后续可在边界场景叠加 LLM 解释层。

---

## 5. Agent 架构

### 5.1 Agent 拆分（按 Prompt + Tools + Context 区分）
| Agent | 职责 |
|-------|------|
| Planner Agent | 理解输入 → 拆解 Goal → 生成 Task Candidate → 调 Scheduler |
| Replanner Agent | 读取当前计划与变化原因 → 调 Scheduler → 解释调整结果 |
| Goal Agent | 长期目标拆解（Goal → Milestone → Task） |
| Review Agent | 每日/每周复盘，更新 UserTimeProfile |
| Conversation Agent | 与用户对话、解释系统行为 |

> 实际可复用同一 LLM，区别仅在 Prompt / Tools / Context。

### 5.2 Agent Tool Layer（Agent 唯一入口）
Agent → Tool → Go Service → DB

必须提供的工具：
- `GetTodaySchedule()` / `GetAvailableTime()` / `GetTasks()` / `GetGoals()`
- `CreateTask()` / `UpdateTask()` / `ScheduleTask()`
- `ReplanDay()` / `GetExecutionHistory()` / `GetUserTimeProfile()`
- `StartTimer()` / `CompleteTask()`

---

## 6. 系统职责边界

### 6.1 Go 后端必须负责
- Timer 真实状态
- Task 状态机
- 时间冲突检测
- 任务依赖图
- 权限与多人状态
- 数据持久化
- Scheduler
- 通知与统计

### 6.2 Agent 可以负责
- 自然语言理解
- 目标拆解建议
- 任务建议
- 计划解释
- 变化原因归因
- 每日/长期总结

---

## 7. Go 后端目录结构
```
internal/
 ├─ auth/
 ├─ user/
 ├─ goal/
 ├─ milestone/
 ├─ task/
 ├─ schedule/
 │   ├─ scheduler.go
 │   ├─ planner.go
 │   ├─ replanner.go
 │   ├─ conflict.go
 │   ├─ priority.go
 │   └─ availability.go
 ├─ timer/
 │   ├─ timer.go
 │   └─ session.go
 ├─ execution/
 ├─ team/
 │   ├─ team.go
 │   ├─ member.go
 │   ├─ task.go
 │   └─ session.go
 ├─ notification/
 ├─ email/
 ├─ calendar/
 ├─ analytics/
 └─ agent/
     ├─ planner/
     ├─ replanner/
     ├─ review/
     ├─ goal/
     ├─ tools/
     ├─ repository/
     └─ service/
```

---

## 8. 核心数据库表（第一阶段）
```
users
goals
milestones
tasks
task_dependencies
daily_plans
schedule_blocks
timer_sessions
task_executions
user_time_profiles
teams
team_members
team_tasks
team_sessions
notifications
agent_conversations
agent_actions
```

---

## 9. API v1 范围
```
Auth
  POST /auth/register
  POST /auth/login

Goal
  POST   /goals
  GET    /goals
  GET    /goals/:id
  PATCH  /goals/:id

Task
  POST   /tasks
  GET    /tasks
  GET    /tasks/:id
  PATCH  /tasks/:id
  POST   /tasks/:id/complete
  POST   /tasks/:id/skip

DailyPlan
  GET    /plans/today
  POST   /plans/generate
  POST   /plans/replan

Timer
  POST   /tasks/:id/timer/start
  POST   /tasks/:id/timer/pause
  POST   /tasks/:id/timer/resume
  POST   /tasks/:id/timer/complete
  GET    /tasks/:id/timer

Agent
  POST   /agent/chat
  POST   /agent/plan
  POST   /agent/replan
```

---

## 10. 分阶段实施计划（9 个 Phase）

> 原则**：先保证**任务能安排、时间能计时、计划能调整、系统能学习、多人能协作**，再做周边。

### Phase 1 — 基础任务系统（不接 Agent）
**目标**：跑通 User → Goal → Milestone → Task 全链路。

交付物：
- User / Auth（JWT）
- Goal / Milestone / Task CRUD
- Task 状态机（PENDING / SCHEDULED / COMPLETED 等基础态）
- 数据库（核心表迁移）
- API v1 基础端点

**完成判据**：能注册、登录、建 Goal、拆 Milestone、建 Task、修改状态。

---

### Phase 2 — Timer Engine
**目标**：Timer 真实、可靠、可恢复。

**交付物**：
- Start / Pause / Resume / Complete
- `actual_duration` / `overtime` 计算
- `TaskExecution` 落库
- 服务端为计时权威，客户端只读 `started_at / paused_at / total_pause_duration`

**完成判据**：杀掉 App / 换设备 / 断网后状态可恢复；同一 Task 多次执行可形成执行历史。

---

### Phase 3 — Scheduler Engine（确定性版本）
**目标**：基于规则生成每日 `ScheduleBlock`。

**交付物**：
- Available Time 计算
- Fixed Event 叠加
- Priority / Deadline / Estimated Duration 排序
- ScheduleBlock 输出
- Conflict Detection

**完成判据**：给定任务集合与可用时间，能稳定输出无冲突的时间块序列。

---

### Phase 4 — Dynamic Replanning
**目标**：触发条件 → 自动重排当天计划。

**交付物**：
- 触发器：超时 / 提前完成 / 跳过 / 新增 / 删除 / 可用时间变化 / Calendar 变化 / Team Task 变化
- 增量重排算法（不每次从头算）
- 调整原因解释输出

**完成判据**：模拟任意一种触发，都能产出新的 ScheduleBlock 并记录 diff。

---

### Phase 5 — Agent 层接入
**目标**：自然语言成为一等输入。

**交付物**：
- Agent Tool Layer（Go 侧 Tools）
- Planner Agent：NL → Task Candidate + 调 Scheduler
- Replanner Agent：变化归因 + 调 Scheduler
- Goal Agent：长期目标拆解
- `POST /agent/chat` / `/agent/plan` / `/agent/replan`

**完成判据**：
- 输入「今天下午 2 点到 6 点有空，必须写完 JWT，还想看一小时 Redis」能自动生成 ScheduleBlock。
- 任务超时后 Agent 能用自然语言告诉用户发生了什么。

---

### Phase 6 — 时间学习（Adaptive Duration）
**目标**：让 estimated_duration 从静态变成自学习。

**交付物**：
- `TaskExecution` 聚合统计
- `UserTimeProfile`（按 task_type）
- 修正后的 `estimated_duration` 回写
- 闭环：执行 → 统计 → 修正 → 重规划

**完成判据**：连续执行后，Scheduler 输入的 `estimated_duration` 自动收敛到用户真实节奏。

---

### Phase 7 — 多人协作
**目标**：从单人扩展到 Team。

**交付物**：
- Team / Member / Shared Task / Team Session
- Shared Goal + 任务依赖图
- Team Focus Session（90 分钟同步专注）

**完成判据**：3 人组队 → 创建 Shared Goal → 拆解并领取 → 阻塞依赖能自动提醒。

---

### Phase 8 — 通知与邮件
**目标**：让用户不在 App 里也能被服务。

**交付物**：
- Android Notification
- Email（Morning Plan / Daily Summary / Weekly Summary）
- In-App Notification

**完成判据**：
- 早上收到 Today Plan 邮件
- 晚上收到 Daily Summary + 周报

---

### Phase 9 — Calendar 集成
**目标**：把外部日历纳入可用时间计算。

**交付物**：
- Calendar Import（Google / ics 等）
- Busy Blocks → Available Time
- Scheduler 自动避让

**完成判据**：日历里的会议自动变成 Fixed Schedule，不被覆盖。

---

## 11. MVP 验收用例（必须一次性走通）

输入：
> 我今天下午 2 点到 6 点有时间。必须写完 JWT。还想看一小时 Redis。这个星期希望完成 Go 用户系统。

期望输出（ScheduleBlock）：
```
14:00-15:20  JWT
15:20-15:30  Break
15:30-16:20  User Service
16:30-17:30  Redis
```

执行验证：
- 用户开启 JWT Timer
- 计划 80min，实际 107min（超时 27min）
- 系统自动重排：
  - User Service: 16:00 → 16:20
  - Redis: 17:00 → 推到明天
- 第二天系统按历史把 Coding 类任务 `estimated_duration` 从 80min 修正为 100min

**以上完整闭环跑通 = MVP 完成**。

---

## 12. 路线图（时间维度建议）

| 阶段 | 周期 | 关键里程碑 |
|------|------|-----------|
| Phase 1 | 1–2 周 | 登录 + 建任务 |
| Phase 2 | 1 周 | Timer 可恢复 |
| Phase 3 | 2 周 | Scheduler 确定性版上线 |
| Phase 4 | 1 周 | Replanning 闭环 |
| Phase 5 | 2 周 | 自然语言输入可用 |
| Phase 6 | 1–2 周 | 时间学习生效 |
| Phase 7 | 2 周 | Team MVP |
| Phase 8 | 1 周 | 邮件 + 通知 |
| Phase 9 | 1–2 周 | Calendar 集成 |

> 时间仅供参考，可按人手与优先级灵活调整；不建议跳过 Scheduler 直接做 Agent。

---

## 13. 暂不开发的功能
- 皮肤、等级、商城、虚拟角色等与核心闭环无关的特性。
- 任何不服务「输入 / 规划 / 执行 / 计时 / 分析 / 重规划 / 协作」的功能。

---

## 14. 下一步立即要做（Phase 1 Kickoff）

1. 初始化 Go 项目（`go mod init github.com/<org>/questos`），按本文 §7 创建 `internal/` 目录骨架。
2. 选定 Web 框架（Gin / Echo / Chi 二选一）+ ORM（GORM / entsql + bun + ent）。
3. 选定 DB（PostgreSQL 优先，JSONB 存灵活配置）。
4. 设计并落地 Phase 1 数据库迁移（users / goals / milestones / tasks / task_dependencies）。
5. 实现 `POST /auth/register` / `/login` + JWT 鉴权中间件。
6. 实现 Goal / Milestone / Task 基础 CRUD。
7. 接入一套基础测试（service / repo / handler 三层单测）。
8. 输出 `docs/architecture.md` 与本文档保持同步。

---

_本文档是 QuestOS 的开发主线。任何 PR / 设计决策都必须能在本文找到归属，否则不应合并。_